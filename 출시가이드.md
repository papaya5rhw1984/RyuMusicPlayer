# RyuMusicPlayer 출시 가이드 (네이티브 Android)

> ⚠️ 이 앱은 **네이티브(Kotlin)** 앱입니다. 기존 PWA 방식(PWABuilder · GitHub Pages · assetlinks)은 쓰지 않습니다.
> 대신 **Android Studio에서 서명된 AAB를 빌드해 스토어에 직접 업로드**합니다.

## 기본 정보
- 앱 이름: Ryu Music
- 패키지ID(applicationId): `io.github.papaya5rhw1984.ryumusic`  ← 출시 후 변경 불가
- 코드 패키지(namespace): `com.ryu.musicplayer`
- versionCode 1 / versionName 1.0
- minSdk 24 / targetSdk 34
- 개인정보처리방침 URL(공용 재사용): https://papaya5rhw1984.github.io/privacy/
- 문의 이메일: papaya5rhw@naver.com

## 1) 업로드 키스토어 만들기 (최초 1회)
PowerShell/터미널에서 (keytool은 JDK에 포함):
```
keytool -genkeypair -v -keystore ryumusic-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias ryumusic
```
- 생성된 `ryumusic-release.jks` 는 `store_원본파일(지우면안돼_키있음)\ryumusic\` 에 보관(분실 시 앱 업데이트 영원히 불가 — 절대 잃어버리지 말 것).

## 2) keystore.properties 작성
- `keystore.properties.example` 를 복사 → `keystore.properties` 로 저장 → 경로·비밀번호 채우기.
- 이 파일과 .jks 는 git에 올라가지 않음(.gitignore 처리됨).

## 3) 서명된 AAB 빌드
Android Studio: **Build ▸ Generate Signed App Bundle / APK ▸ Android App Bundle**
- 키스토어 선택 → release 빌드.
- 또는 터미널: `./gradlew bundleRelease` → `app/build/outputs/bundle/release/app-release.aab`

## 4) 스토어 등록 (원스토어 / 플레이)
- AAB 업로드
- 개인정보처리방침 URL: 위 공용값 붙여넣기
- 데이터 안전: **수집·전송하는 데이터 없음** (기기 내 오디오만 재생, 외부 전송 없음)
- 권한 설명: 음악/오디오 접근(READ_MEDIA_AUDIO)은 기기 음악 재생 목적
- 스크린샷 2~8장, 아이콘(512), 설명, 카테고리=음악/오디오, 연령등급 설문
- `_출시관리.md` 대장에 한 줄 추가

## 체크리스트
- [ ] 키스토어 생성·백업
- [ ] keystore.properties 작성
- [ ] 서명된 AAB 빌드 성공
- [ ] 실기기에서 release 빌드 동작 확인
- [ ] 스토어 자료(스크린샷·설명·연령등급)
- [ ] 개인정보 URL 입력
