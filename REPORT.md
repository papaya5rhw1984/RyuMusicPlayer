# 류 뮤직 플레이어 — REPORT

## v1.2.0 (2026-06-12 2회차, versionCode 5)
이전(v1.1.0) 대비:

1. **[콘텐츠] 수면 타이머 '곡 끝' 모드** — 순환이 끄기→15→30→60분→**곡 끝**→끄기로 확장. 곡이 자연 종료(AUTO 전환)되는 순간 일시정지, 수동 스킵은 모드 유지(다음 곡 끝에 정지). 라벨 "🌙 곡 끝".
2. **[UX] ±10초 탐색** — 컨트롤 아래 "↺ 10초 / 10초 ↻" 버튼, 0~곡 길이로 클램프.
3. **[QA]** duration 미확정(<=0) 시 상한 클램프 생략 가드, 곡끝 모드와 분 타이머 상호 배타 보장.

### 검증 (v1.2.0): ✅ 상태 순환 로직 검토(곡끝↔분 타이머 배타), 편집 헝크 괄호 짝 확인 / ❌ 에뮬레이터 실행 못 함(샌드박스 한계). **실행 확인: 타이머 5탭 순환, 곡 끝 모드에서 자연 전환 시 정지.**
### 자체 점수: 90 → 92/100

## v1.1.0 (2026-06-12, versionCode 4)
이전(v1.0.2) 대비 변경점 (역할 순차):

1. **[콘텐츠] 수면 타이머 🌙** — 컨트롤 줄 버튼, 탭마다 끄기→15→30→60분 순환. 남은 분 표시, 시간이 되면 자동 일시정지. 위치 루프(400ms)에 통합해 별도 타이머 잡 없음.
2. **[콘텐츠] 이어듣기** — 기존엔 마지막 곡만 복원하고 위치는 0초. 재생 중 4초마다 위치 저장(`lastPos`) → 재시작 시 같은 곡·같은 위치에서 일시정지 복원. 곡 전환 시 lastPos 리셋으로 위치-곡 불일치 방지.
3. **[QA] 재생속도 저장·복원** — 기존엔 재시작 시 1x로 초기화(표시는 저장 안 된 speedIndex와 불일치 가능). `speedIdx` 저장, controller 연결 시 복원·적용. 인덱스 범위 가드.
4. **[디자인] 타이머 버튼** — 배속 버튼과 동일한 Vinyl 스타일, 작동 중 Accent 색 + 남은 분.

### 데이터 마이그레이션
- 기존 키(shuffle/repeat/lastTrack/lastChip) 불변. 신규 키 lastPos·speedIdx 추가 전용 — 무손실.

### 추가 수정 (같은 날 2차 — "재실행 시 음악 유지 안 됨" 버그 원인 수정)
- **원인 ①**: onStop에서 MediaController를 해제하기 때문에 백그라운드 재생·강제 종료 시점의 곡/위치가 기록되지 않음 → **onStop 직전 `saveNowState()` 스냅샷 저장** 추가.
- **원인 ②**: 듣던 재생목록에서 곡이 빠졌거나 목록이 삭제되면 복원을 그냥 포기(`idx<0 → return`) → **'전체' 목록에서 재탐색 폴백** 추가.
- **원인 ③(신규 코드 자체 결함)**: 복원 직후 발생하는 onMediaItemTransition이 lastPos를 0으로 리셋 → **곡이 실제로 바뀐 경우에만 리셋**하도록 가드.

### 검증
- ✅ before/after: 재시작 시 곡만 복원·0초 → 곡+위치 복원 / 배속 초기화 → 유지 / 수면 타이머 부재 → 15/30/60분.
- ✅ StateFlow 동일값 재방출 없음 특성 활용해 분 단위 갱신 부하 0 확인(설계 검토).
- ❌ 에뮬레이터 실행·Logcat 못 함(샌드박스 SDK 없음·네트워크 차단). **Android Studio 실행 확인 필요(이어듣기·타이머 동작).**

### 자체 점수: 87 → 90/100

### 출시 리스크 / 다음
- 곡 끝까지 듣고 종료(트랙 끝 타이머)·이퀄라이저는 v1.2 후보. 출시본은 같은 키스토어로 AAB 재빌드 (versionCode 4).
